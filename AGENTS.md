# Agent Rules

请使用中文交流。

修改任何文件前：

- 先分析问题
- 先给出方案
- 等待我明确表达要执行或修改的意思

只有当我明确表达要执行或修改的意思时，才允许修改文件；不要求使用固定词，比如 `APPROVE`。

当我明确使用 `/goal` 模式并限定任务范围时，可以在当前项目目录内自主修改文件和运行测试。

即使处于 `/goal` 模式，仍然禁止：

- 删除文件
- 安装依赖
- 执行 `git push`
- 修改项目目录外的内容

修改代码后：

- 运行相关测试
- 如果无法运行测试，需要说明原因

## harness 能力与治理维度

1. 指令与上下文
2. Agent 循环与任务规划
3. 工具调用与工具路由
4. 权限、审批与安全策略
5. 状态、会话与记忆
6. 沙盒与执行环境
7. 验证与结果检查
8. 可观测性与审计
9. 多 Agent 协作
10. 生产级运行保障
